# Copy to .env (already set in nix devShell too)
DATABASE_URL=postgres://challanger:challanger@localhost:5433/challanger
BIND_ADDR=127.0.0.1:3000
RUST_LOG=challanger=debug,tower_http=debug,info
JWT_SECRET=dev-secret-challanger-2026

# Telegram bot (ixtiyoriy) — @BotFather'dan olingan token.
# Berilmasa bot o'chirilgan holatda qoladi, ilova baribir ishlaydi.
TELEGRAM_BOT_TOKEN=
